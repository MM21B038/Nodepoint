import time


def preprocess(workspace_name, workspace_path):

    print(f"Starting preprocessing for {workspace_name}")

    # Simulate heavy task
    time.sleep(10)

    print(f"Finished preprocessing for {workspace_name}")